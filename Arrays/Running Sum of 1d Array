class Solution {
public:
    vector<int> runningSum(vector<int>& nums) {
        int n=nums.size();
        int a=0;
        for(int i=0;i<n;i++){
            nums[i]=nums[i]+a;
            a=nums[i];
        }
        return nums;
        
    }
};
